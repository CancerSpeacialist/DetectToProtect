import SignUpForm from '@/components/auth/SignUpForm'


export const metadata = {
  title: "Sign Up | Detect to Protect",
  description: "Create your account for Detect to Protect",
};

export default function SignUpPage() {
  return <SignUpForm />;
}