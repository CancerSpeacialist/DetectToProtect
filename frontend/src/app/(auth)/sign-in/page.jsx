import SignInForm from '@/components/auth/SignInForm';

export const metadata = {
  title: 'Sign In | Detect to Protect',
  description: 'Sign in to your Detect to Protect account',
};

export default function SignInPage() {
  return <SignInForm />;
}